plugins {
    id 'java'
    id 'application'
    id 'java-conventions'
}

dependencies {
    implementation(project(':framework'))
    implementation(project(':capitaliser'))
    implementation(project(':tokeniser'))
}

